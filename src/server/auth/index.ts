/**
 * @module for-http/server/auth
 * @summary Server auth primitives.
 * @description Framework-agnostic authorization base class and structural types that
 * form the base contract for all Decaf server-side auth integrations.
 */
export * from "./types";
export * from "./AuthHandler";
